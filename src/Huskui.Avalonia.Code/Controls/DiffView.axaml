<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
    xmlns:code="clr-namespace:Huskui.Avalonia.Code.Converters">

    <ControlTheme
        x:Key="{x:Type husk:DiffView}"
        TargetType="husk:DiffView">
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderBrush="{StaticResource ControlBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource SmallCornerRadius}">
                    <Panel>
                        <ScrollViewer
                            x:Name="{x:Static husk:DiffView.PART_ScrollViewer}"
                            HorizontalScrollBarVisibility="Disabled"
                            VerticalScrollBarVisibility="Hidden">
                            <ItemsControl ItemsSource="{TemplateBinding Lines}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="husk:DiffLineModel">
                                        <Grid MinHeight="{x:Static husk:DiffView.LINE_HEIGHT}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="1" />
                                                <ColumnDefinition Width="48" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border
                                                Grid.Column="0"
                                                Background="{Binding LeftKind, Converter={x:Static code:InternalConverters.DiffLineKindToBackground}}"
                                                BorderBrush="{StaticResource ControlBorderBrush}"
                                                BorderThickness="0,0,1,0">
                                                <Panel>
                                                    <Border
                                                        Width="3"
                                                        HorizontalAlignment="Left"
                                                        Background="{Binding LeftKind, Converter={x:Static code:InternalConverters.DiffLineKindToIndicatorBrush}}" />
                                                    <TextBlock
                                                        Margin="8,2"
                                                        HorizontalAlignment="Right"
                                                        VerticalAlignment="Center"
                                                        FontFamily="Cascadia Code, Consolas, Courier New, monospace"
                                                        FontSize="{x:Static husk:DiffView.MEASURE_FONT_SIZE}"
                                                        Foreground="{Binding LeftKind, Converter={x:Static code:InternalConverters.DiffLineKindToForeground}}"
                                                        Text="{Binding LeftLineNumber}" />
                                                </Panel>
                                            </Border>
                                            <Border
                                                Grid.Column="1"
                                                Background="{Binding LeftKind, Converter={x:Static code:InternalConverters.DiffLineKindToBackground}}"
                                                ClipToBounds="True">
                                                <Border
                                                    Width="{Binding $parent[husk:DiffView].ContentWidth}"
                                                    HorizontalAlignment="Left">
                                                    <Border.RenderTransform>
                                                        <TranslateTransform X="{Binding $parent[husk:DiffView].HorizontalOffset}" />
                                                    </Border.RenderTransform>
                                                    <TextBlock
                                                        Margin="8,2"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        FontFamily="Cascadia Code, Consolas, Courier New, monospace"
                                                        FontSize="{x:Static husk:DiffView.MEASURE_FONT_SIZE}"
                                                        Text="{Binding LeftText}"
                                                        TextWrapping="NoWrap" />
                                                </Border>
                                            </Border>
                                            <Border
                                                Grid.Column="2"
                                                Background="{StaticResource ControlBorderBrush}" />
                                            <Border
                                                Grid.Column="3"
                                                Background="{Binding RightKind, Converter={x:Static code:InternalConverters.DiffLineKindToBackground}}"
                                                BorderBrush="{StaticResource ControlBorderBrush}"
                                                BorderThickness="0,0,1,0">
                                                <Panel>
                                                    <Border
                                                        Width="3"
                                                        HorizontalAlignment="Left"
                                                        Background="{Binding RightKind, Converter={x:Static code:InternalConverters.DiffLineKindToIndicatorBrush}}" />
                                                    <TextBlock
                                                        Margin="8,2"
                                                        HorizontalAlignment="Right"
                                                        VerticalAlignment="Center"
                                                        FontFamily="Cascadia Code, Consolas, Courier New, monospace"
                                                        FontSize="{x:Static husk:DiffView.MEASURE_FONT_SIZE}"
                                                        Foreground="{Binding RightKind, Converter={x:Static code:InternalConverters.DiffLineKindToForeground}}"
                                                        Text="{Binding RightLineNumber}" />
                                                </Panel>
                                            </Border>
                                            <Border
                                                Grid.Column="4"
                                                Background="{Binding RightKind, Converter={x:Static code:InternalConverters.DiffLineKindToBackground}}"
                                                ClipToBounds="True">
                                                <Border
                                                    Width="{Binding $parent[husk:DiffView].ContentWidth}"
                                                    HorizontalAlignment="Left">
                                                    <Border.RenderTransform>
                                                        <TranslateTransform X="{Binding $parent[husk:DiffView].HorizontalOffset}" />
                                                    </Border.RenderTransform>
                                                    <TextBlock
                                                        Margin="8,2"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        FontFamily="Cascadia Code, Consolas, Courier New, monospace"
                                                        FontSize="{x:Static husk:DiffView.MEASURE_FONT_SIZE}"
                                                        Text="{Binding RightText}"
                                                        TextWrapping="NoWrap" />
                                                </Border>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                        <husk:DiffOverviewBar
                            x:Name="{x:Static husk:DiffView.PART_OverviewBar}"
                            Width="{x:Static husk:DiffOverviewBar.DEFAULT_WIDTH}"
                            Margin="2"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Stretch"
                            IsVisible="{Binding $parent[husk:DiffView].IsOverviewBarVisible}"
                            Markers="{Binding $parent[husk:DiffView].Markers}"
                            TotalLines="{Binding $parent[husk:DiffView].TotalLineCount}"
                            ViewTopRatio="{Binding $parent[husk:DiffView].OverviewTopRatio}"
                            ViewportRatio="{Binding $parent[husk:DiffView].OverviewViewportRatio}" />
                        <ScrollBar
                            x:Name="{x:Static husk:DiffView.PART_HScrollBar}"
                            VerticalAlignment="Bottom"
                            Orientation="Horizontal"
                            Visibility="Hidden" />
                    </Panel>
                </Border>
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary>
